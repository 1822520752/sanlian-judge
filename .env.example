# sanlian-judge - 环境变量示例
# 复制本文件为 .env 并填入真实值,.env 已加入 .gitignore 不会被提交

# StepFun(阶跃星辰)API Key (必填) - https://platform.stepfun.com/
# 也可使用 STEP_API_KEY 或 STEPFUN_TOKEN
STEPFUN_API_KEY=

# Vercel KV / Netlify Blobs 存储 (可选) - 不填则降级为本地 data/rank.json 兜底
KV_REST_API_URL=
KV_REST_API_TOKEN=
